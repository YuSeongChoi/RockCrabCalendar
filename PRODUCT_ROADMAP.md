# RockCrabCalendar 제품 개선 로드맵

## 1. 문서 목적

이 문서는 현재 `RockCrabCalendar`의 기능 상태를 기준으로, 다음 단계에서 무엇을 추가하거나 개선할지 제품 관점에서 정리한 기획 문서입니다.

목표는 세 가지입니다.

1. 지금 있는 기능 중 어떤 부분이 실사용 가치를 만들고 있는지 명확히 정의한다.
2. 다음 1~3개월 동안 우선순위가 높은 개선 항목을 선정한다.
3. 구현 난이도보다 `재방문 이유`, `운영 안정성`, `팬앱으로서의 차별성`을 우선하는 로드맵을 만든다.

---

## 2. 현재 제품 상태 요약

### 현재 강점

- 공식 일정(QWER)과 개인 일정을 같은 캘린더에서 함께 볼 수 있다.
- 월간 캘린더와 월간 리스트 뷰를 모두 제공한다.
- 카테고리 기반 필터링이 가능하다.
- 사용자가 직접 개인 일정과 QWER 일정을 추가할 수 있다.
- 위젯을 통해 홈 화면에서 주요 일정을 빠르게 확인할 수 있다.
- 앱 내부 언어 설정과 ICS 내보내기를 지원한다.

### 현재 한계

- 일정을 “보는” 경험은 좋지만, “놓치지 않게 해주는” 경험은 아직 약하다.
- 일정 상세에서 실제 행동으로 이어지는 링크 연결이 없다.
- 필터는 있지만 개인화된 기본 구독/기본 노출 설정은 없다.
- 검색이 없어 일정 수가 늘어날수록 탐색 비용이 증가한다.
- 지난 일정이 쌓여도 회고나 기록으로 이어지는 장치가 없다.

---

## 3. 현재 알림 기능 기준선

현재 구현은 이미 존재하며, 아래 전제를 제품 기획의 기준선으로 삼습니다.

- 일정 편집 화면에서 `시작 전에 알림 받기` 토글을 켤 수 있다.
- 알림은 로컬 알림 기반이다.
- 일정별로 알림 시점을 직접 선택할 수 있다.
- 현재 선택지는 `1시간 전`, `30분 전`, `10분 전`, `5분 전`이다.
- 기본 선택값은 `10분 전`이다.
- 알림은 선택한 시점 기준으로 1회 예약된다.
- 하루종일 일정 또는 시간이 미정인 일정은 알림 대상에서 제외된다.

구현 참고 파일:

- [NotificationManager.swift](/Users/yuseongchoi/Desktop/RockCrabCalendar/RockCrabCalendar/RockCrabCalendar/Presentation/Core/Service/NotificationManager.swift:1)
- [ScheduleEditBasicInfoSection.swift](/Users/yuseongchoi/Desktop/RockCrabCalendar/RockCrabCalendar/RockCrabCalendar/Presentation/Features/Home/Schedule/View/Components/ScheduleEditBasicInfoSection.swift:1)

### 현재 구조에 대한 평가

지금 상태는 “최소 기능(MVP)”으로는 충분합니다.

- 장점: 일정별 맥락에 맞춰 알림 시점을 고를 수 있고, 생성/수정/삭제 흐름 안에서 동작이 예측 가능하다.
- 장점: 전역 설정 없이 일정 단위에서 바로 결정하므로 사용자가 이해하기 쉽다.
- 단점: 여러 일정에 공통으로 적용할 기본 알림 프리셋은 아직 없다.
- 단점: 알림을 언제 왜 못 받는지에 대한 상세 피드백은 최소화된 상태다.

따라서 다음 단계에서는 곧바로 더 복잡한 알림 옵션을 늘리기보다, 아래 순서를 추천합니다.

1. 현재 일정별 알림 선택 방식이 실제로 충분한지 확인한다.
2. 반복적으로 선택되는 시점 패턴이 있는지 피드백을 모은다.
3. 필요성이 확인되면 그다음에 전역 프리셋이나 카테고리별 기본값을 검토한다.

---

## 4. 제품 우선순위 기준

다음 기능 우선순위는 아래 4가지 기준으로 판단합니다.

### A. 재방문 이유를 만드는가

사용자가 앱을 다시 열어야 할 명확한 이유가 생기는 기능이 우선입니다.

### B. 팬활동 흐름을 실제로 돕는가

예쁘거나 보기 좋은 기능보다 “놓치지 않게 한다”, “바로 행동하게 한다”, “정리하기 쉽게 만든다”가 우선입니다.

### C. 운영 안정성을 해치지 않는가

외부 연동, 푸시, 공유, 동기화 기능은 편리하지만 회귀 위험이 커서 단계적으로 도입해야 합니다.

### D. 현재 구조에서 자연스럽게 확장 가능한가

지금 코드베이스의 캘린더/일정/위젯 구조를 크게 깨지 않고 들어갈 수 있는 기능을 우선합니다.

---

## 5. 추천 로드맵

## Phase 1. “놓치지 않게 해주는 앱”으로 정리

기간 추천: 2~4주

### 5-1. 알림 UX 정리

#### 목표

현재 있는 알림 기능을 사용자 입장에서 더 이해하기 쉽게 만들고, 실사용 가치를 확인한다.

#### 문제

기본 MVP는 완료됐지만, 앞으로 확장할지 유지할지를 판단할 기준이 아직 없습니다.

#### 개선안

- 현재 일정별 알림 시각 선택 구조를 유지한다.
- 실제 사용자 피드백을 기준으로 자주 쓰는 시각이 있는지 확인한다.
- 필요 시 다음 단계에서 전역 프리셋 또는 카테고리별 기본 알림 규칙을 검토한다.

#### 이 단계에서 하지 않는 것

- 더 많은 알림 시점 옵션 추가
- 카테고리별 알림 시간 분기
- Time Sensitive 알림

#### 기대 효과

- 현재 구조를 과도하게 복잡하게 만들지 않고 유지할 수 있음
- 실제 사용 패턴이 확인된 뒤 다음 알림 기능을 설계할 수 있음

### 5-2. 일정 상세 화면 정리 (보류)

#### 목표

일정을 단순히 보는 수준에서 벗어나, 한 번 탭했을 때 필요한 행동으로 바로 이어지게 만든다.

#### 현재 판단

현재 제품 상태에서는 우선순위를 낮게 본다.

- 지금은 일정 정보량이 많지 않아 상세 화면이 새로운 가치를 만들기 어렵다.
- 잘못 넣으면 `목록/캘린더 -> 상세 -> 수정`으로 depth만 1단계 늘어날 수 있다.
- 링크, 기록, 공유처럼 상세 화면에서만 의미 있는 기능이 충분히 생길 때 다시 검토하는 편이 낫다.

#### 핵심 항목

- 제목
- 날짜/시간
- 장소
- 카테고리
- 멤버 태그
- 메모 또는 설명
- 외부 링크 영역(향후 확장 대비)

#### 권장 구현 범위

- `ScheduleDetailView` 신설
- QWER 일정과 개인 일정이 공통으로 표시할 수 있는 읽기 전용 상세 UI 구성
- 홈 캘린더 셀, 월간 리스트, 위젯 진입부에서 상세 화면으로 연결 가능한 구조 마련
- 현재 있는 수정/삭제 흐름과 충돌하지 않도록 `상세 -> 편집` 이동 구조 분리

#### 1차 화면 구성 제안

- 상단 요약 카드
  - 제목
  - 날짜
  - 시간
  - 장소
- 메타 정보 섹션
  - 카테고리
  - 멤버
  - 알림 여부
- 추가 정보 섹션
  - 메모 또는 설명
  - 향후 외부 링크가 들어갈 자리
- 하단 액션
  - 편집
  - 삭제(사용자 수정 가능한 일정만)

#### 데이터 설계 제안

- 우선은 기존 `QWERScheduleItem`, `UserScheduleItem`을 그대로 사용한다.
- 공통 표시값이 필요하면 상세 화면 전용 ViewData 또는 Mapper를 추가한다.
- 링크/메모처럼 아직 모델에 없는 값은 UI placeholder 수준만 먼저 두고, 실제 데이터 모델 변경은 5-3과 함께 진행한다.

#### 개발 순서 제안

1. 공통 상세 화면 레이아웃 정의
2. QWER 일정 연결
3. 개인 일정 연결
4. 편집 진입 연결
5. 삭제 가능 조건 정리

#### 기대 효과

- 리스트/캘린더에서 보지 못한 추가 정보를 담을 수 있음
- 이후 링크, 회고, 공유 기능의 기반이 됨

---

## Phase 2. “팬활동에 실제로 도움되는 앱”으로 확장

기간 추천: 4~8주

### 5-3. 일정별 링크 연결 (보류)

#### 목표

일정 정보가 실제 행동으로 바로 이어지게 만든다.

#### 현재 판단

현재 제품 상태에서는 우선순위를 낮게 본다.

- 링크 기능은 읽기 전용 상세 화면과 함께 설계될 가능성이 높다.
- 어떤 링크를 누가, 어떤 기준으로 넣고 운영할지 정책이 먼저 정해져야 한다.
- QWER 일정 운영 방식까지 포함하면 데이터 관리 부담이 커질 수 있다.
- 따라서 지금은 기능 자체보다 검색처럼 즉시 가치가 큰 기능을 우선하는 편이 낫다.

#### 예시

- 팬사인회 응모 링크
- 티켓 예매 링크
- 위버스 공지 링크
- 유튜브 공개 링크
- 장소 지도 링크

#### 권장 방식

- 일정 모델에 `externalLinks: [ScheduleExternalLink]` 구조 추가
- 일정 상세 화면에서 버튼 형태로 노출
- 링크가 없으면 영역 자체를 숨김

#### 기대 효과

- 앱 안에서 “확인 후 이동”이 가능해짐
- 팬 입장에서 정보 허브 역할 강화

### 5-4. 기본 구독/기본 필터 설정 (보류)

#### 목표

매번 필터를 다시 고르지 않아도, 사용자가 관심 있는 일정만 기본 노출되게 만든다.

#### 현재 판단

현재 제품 상태에서는 우선순위를 낮게 본다.

- QWER 일정은 멤버 전체가 함께 등장하는 일정 비중이 높다.
- 현재 데이터 특성상 개인화된 기본 필터가 큰 체감가치를 만들 가능성이 낮다.
- 현재 필터 기능만으로도 사용성은 일정 수준 확보되어 있다.

#### 예시

- 공연만 기본 보기
- 팬사인회/생일만 보기
- 특정 멤버 일정만 우선 보기

#### 권장 방식

- 설정 화면에 “기본 일정 필터” 추가
- 홈 진입 시 저장된 기본 필터 적용
- 현재 필터 UI는 유지하되 기본값만 개인화

#### 기대 효과

- 매일 여는 앱으로서의 효율 상승
- 팬 성향별 맞춤 경험 제공

### 5-5. 검색

#### 목표

일정 수가 많아져도 빠르게 원하는 항목을 찾게 한다.

#### 현재 판단

검색 MVP는 구현 완료로 본다.

- 현재 화면 구조를 크게 깨지 않고 적용할 수 있었다.
- 캘린더 모드와 리스트 모드의 역할을 분리한 채 검색 진입 흐름을 만들 수 있었다.
- 일정이 누적될수록 가치가 더 커질 가능성이 높다.

#### 검색 대상

- 일정 제목
- 장소
- 멤버
- 카테고리

#### 권장 구현 범위

- 캘린더 모드에서는 검색바를 숨김
- 리스트 모드에서만 검색바 노출
- 돋보기 버튼을 통해 리스트 전환 + 검색바 노출
- `제목`, `장소`, `멤버`, `카테고리` 기준 통합 검색
- QWER 일정과 개인 일정 모두 검색 대상에 포함
- 검색 결과는 현재 캘린더/리스트 구조 안에서 즉시 반영

#### 기대 효과

- 일정 누적 시 탐색 피로 감소
- 특정 행사/장소 재확인 속도 개선

---

## Phase 3. “기록이 남는 팬앱”으로 차별화

기간 추천: 2~3개월

### 5-6. 일정 완료 후 기록 기능

#### 목표

캘린더 앱을 단순 일정 소비 도구에서 팬 기록 도구로 확장한다.

#### 현재 판단

이 기능은 충분히 차별화 포인트가 될 수 있다.

- QWER 일정을 다녀온 뒤 감정과 사진을 남길 수 있으면 앱의 성격이 `조회용 일정 앱`에서 `팬활동 아카이브 앱`으로 확장된다.
- 단순 메모보다 `기록 탭`을 따로 두는 편이 사용자 입장에서 더 설득력 있다.
- 다만 작성 기능보다 `쌓인 기록을 어떻게 다시 보게 할지`가 더 중요하다.

#### 방향 정리

- 별도 `기록 탭`을 신설한다.
- 탭 이름은 `기록`으로 한다.
- 기록은 `QWER 일정` 또는 `개인 일정`과 연결할 수 있다.
- 기록은 일정당 1개만 허용한다.
- 이모지는 기록당 1개로 고정한다.
- 사진은 기록당 최대 5장까지 허용한다.
- 기록은 일정이 삭제되어도 독립된 아카이브로 남길 수 있게 설계하는 편이 낫다.
- 기록 제목은 별도로 받지 않고, 연결된 일정명을 기록의 대표 제목으로 사용한다.

#### 기록 요소 후보

- 이모지 1개
- 회고 텍스트
- 사진 최대 5장
- 연결된 일정 정보
- 작성일 / 수정일

#### 권장 MVP 범위

- 기록 탭 메인 화면
- 기록 상세 화면
- 기록 작성/수정 화면
- 일정 연결
- 이모지 선택
- 회고 텍스트
- 사진 최대 5장 첨부
- 로컬 저장
- 사진 리사이즈 및 압축 저장
- 기록 삭제 및 첨부 사진 파일 정리
- 사진 상세 프리뷰와 확대/축소

#### 조회 화면 설계 원칙

- 기록 수가 100개, 1000개 이상 쌓여도 볼 수 있어야 한다.
- 초기에는 `최신순 타임라인 + 월별 섹션` 구조가 가장 현실적이다.
- 카드에는 이모지 또는 대표 사진 1장, 연결 일정명, 일정 날짜, 회고 본문 일부를 보여준다.
- 상세 화면에서 긴 텍스트와 여러 장의 사진을 조회한다.

#### 데이터 설계 고려사항

- 기록 모델에는 연결 일정 정보의 snapshot을 일부 저장하는 편이 안전하다.
- 예: `scheduleTitleSnapshot`, `scheduleDateSnapshot`
- 원본 일정이 수정되거나 삭제되어도 당시 기록 맥락은 유지되어야 한다.
- 사진은 로컬 저장을 기본으로 보고, 저장 전에 리사이즈/압축을 적용한다.
- 기록 삭제 시 연결된 로컬 사진 파일도 함께 정리한다.

#### 1차 MVP 확정 범위

- 하단 탭에 `기록` 추가
- 기록 리스트 조회
- 기록 상세 조회
- 기록 작성
- 기록 수정
- 일정 1개 연결
- 일정당 기록 1개 제한
- 이모지 1개 선택
- 회고 본문
- 사진 최대 5장
- 로컬 저장
- 사진 리사이즈/압축 후 로컬 파일 저장
- 사진 상세 프리뷰 및 확대/축소
- 기록 삭제 시 연결된 로컬 사진 파일 정리

#### 1차 MVP 비범위

- 기록 검색
- 감정별 필터
- 캘린더형 아카이브
- 원격 동기화
- 통계/연말 회고
- 일정당 여러 기록
- 사진 5장 초과 첨부

#### 후속 개선 후보

- 공유 기능
  - 텍스트 공유: 일정명, 날짜, 이모지, 회고 본문을 문자열로 공유한다.
  - 사진 포함 공유: 첨부 사진과 기록 텍스트를 함께 공유한다.
  - 카드 이미지 공유: 기록을 하나의 예쁜 이미지 카드로 렌더링해 SNS/메신저에 공유한다.
  - 우선순위는 MVP 안정화 이후로 둔다. 공유 결과물이 앱의 팬활동 아카이브 정체성을 강화할 수 있어 장기적으로는 카드 이미지 공유가 가장 적합하다.
- 사진 순서 변경 또는 대표 사진 설정
- 기록 검색
- 감정별 필터
- 캘린더형 아카이브

#### 기대 효과

- 지나간 일정도 가치가 남음
- 앱 잔존율과 감정적 애착 상승
- 팬덤 앱으로서 차별점 형성

---

## 6. 가장 먼저 추천하는 1개 기능

### 추천 1순위: 기록 탭 기반 회고 기능

이유는 아래와 같습니다.

- 팬앱으로서 차별점이 가장 크다.
- 지난 일정이 단순 소비 데이터가 아니라 개인 아카이브가 된다.
- 사진, 감정, 텍스트가 함께 쌓이면 재방문 이유가 강해진다.
- 단순 생산성 기능보다 감정적 애착을 만들 가능성이 높다.

### 구체 작업안

#### 단기 작업

- 기록 탭 정보구조 확정
- 기록 모델 초안 정리
- 기록 리스트 / 상세 / 작성 화면 역할 분리
- 일정 연결 방식 정의
- 사진 저장/압축 정책 정의

#### 다음 단계

- 기록 기능 MVP 구현
- 기록이 쌓인 뒤 검색/감정별 필터/캘린더형 아카이브/공유 기능 도입 여부 판단
- MVP 구현 후 검색/상세/링크와의 연결성 재판단

### 제품 판단

현재는 검색이 완료된 상태이므로, 다음은 기록 기능을 본격적으로 검토하는 것이 가장 자연스럽습니다.

즉, 지금 단계의 권장 전략은 다음과 같습니다.

- 단기: 기록 탭 MVP 구조 설계
- 중기: 기록 작성/조회 흐름 구현
- 장기: 기록 검색, 감정별 보기, 아카이브 확장 검토

---

## 7. 6주 실행 예시

### Week 1

- 알림 UX 정리
- 일정별 알림 시각 구조 적용
- 알림 persistence 및 테스트 보강

### Week 2

- 검색 UI 및 검색 로직 추가
- 제목/장소/멤버/카테고리 검색 연결

### Week 3

- 검색 결과 UX 정리
- 필터와 검색 동시 사용 흐름 정리

### Week 4

- 회귀 테스트
- 사용자 피드백 반영

### Week 5

- 상세 화면/링크 기능 필요성 재검토

### Week 6

- 기록 기능 또는 링크 기능 우선순위 재판단
- 다음 단계 결정

---

## 8. 성공 지표 제안

정량 지표가 아직 없더라도, 아래 항목은 추후 측정 포인트로 정의해둘 가치가 있습니다.

- 일정 생성 후 알림 토글 사용 비율
- 위젯 사용 비율
- 일정 상세 진입 비율
- 검색 사용 비율
- 개인 일정 재등록/수정 비율
- 최근 7일 내 재방문 사용자 비율

---

## 9. 비범위(지금 당장 하지 않을 것)

아래 기능은 매력적이지만 현재 단계에서 우선순위가 낮습니다.

- 커뮤니티/댓글
- 팬 후기 공개 피드
- 복잡한 소셜 기능
- 원격 푸시 서버 구축
- 안드로이드 동시 확장

이 항목들은 운영 비용과 안정성 부담이 커서, 현재 앱의 핵심 경험이 더 단단해진 뒤에 검토하는 것이 좋습니다.

---

## 10. 외부 참고

알림 기획 방향을 잡을 때 참고한 공식 문서:

- Apple Developer: Scheduling a notification locally from your app  
  https://developer.apple.com/documentation/usernotifications/scheduling-a-notification-locally-from-your-app
- Apple Human Interface Guidelines: Managing notifications  
  https://developer.apple.com/design/human-interface-guidelines/managing-notifications

핵심 해석:

- 로컬 알림은 사용자가 실제로 원할 만한 중요한 정보에 써야 한다.
- 알림의 긴급도는 과장하지 말아야 한다.
- 즉시성이 중요한 이벤트에만 더 높은 우선순위 전달을 고려해야 한다.

`RockCrabCalendar`에서는 이 원칙에 따라, 팬사인회/공연 시작 직전 알림처럼 “지금 놓치면 의미가 큰 일정” 중심으로 알림 경험을 설계하는 것이 적절합니다.
